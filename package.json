{
  "name": "prd-assistant",
  "version": "1.0.0",
  "description": "A tool for creating detailed Product Requirements Documents",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && uvicorn main:app --reload",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
